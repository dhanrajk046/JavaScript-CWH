// document is an object that represents the HTML document(DOM).
// window is the global object of the browser so typeof window also returns "object".

// Only works on web browser, not in Node.js
// because document and window are browser-specific objects.