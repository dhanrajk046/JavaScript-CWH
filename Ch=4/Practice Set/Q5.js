// no. Strings are immutable in JavaScript, which means their characters cannot b chnged directly. 

//Example

let str = "Harry";

str[3] = 'z';

console.log(str);

// The string reamins "Harr" because javaScript does not allow modifying individual characters of a string

// If you want to change the fourth character, you must create a new string.

